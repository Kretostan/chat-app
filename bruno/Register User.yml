info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: localhost:3001/auth/register
  body:
    type: json
    data: |-
      {
          "username": "Kuba",
          "email": "email1234@mail.com",
          "password": "trzasłomasło"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
