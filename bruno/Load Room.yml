info:
  name: Load Room
  type: http
  seq: 8

http:
  method: GET
  url: localhost:3001/chat/rooms/:id/messages
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
