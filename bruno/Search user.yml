info:
  name: Search user
  type: http
  seq: 11

http:
  method: GET
  url: localhost:3001/users/search?q=al
  params:
    - name: q
      value: al
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
