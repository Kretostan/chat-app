info:
  name: Load room messages
  type: http
  seq: 10

http:
  method: GET
  url: localhost:3001/chat/rooms/:id/messages
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
