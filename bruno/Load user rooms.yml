info:
  name: Load user rooms
  type: http
  seq: 7

http:
  method: GET
  url: localhost:3001/chat/rooms
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
